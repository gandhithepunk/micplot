services:
  micplot:
    image: ghcr.io/gandhithepunk/micplot:latest
    ports:
      - "3000:3000"
    environment:
      - APP_PASSWORD=
      - ADMIN_PIN=
      - DATABASE_PATH=/app/data/mic-plot.db
      - PHOTOS_DIR=/app/photos
    volumes:
      - mic-plot-data:/app/data
      - mic-plot-photos:/app/photos
    restart: unless-stopped

volumes:
  mic-plot-data:
  mic-plot-photos:

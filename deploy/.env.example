# Copy to .env and fill in.

# Crew login password. Leave blank for no crew auth (fine on a trusted network).
APP_PASSWORD=

# Admin panel PIN (4-6 digits). Leave blank for open admin access.
ADMIN_PIN=
